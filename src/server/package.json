{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node ./src/server.js",
    "start-release": "node ./release/server.js",
    "release": "ncc build src/server.js -o ../../docs -m && npm run rename && npm run version",
    "rename": "node -e \"require('fs').rename('../../docs/index.js', '../../docs/server.js', function() {})\"",
    "version": "node -e \"require('fs').writeFileSync('../../docs/server.js.VERSION.txt', new Date().getTime().toString())\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "crypto-js": "^4.2.0",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "node-localstorage": "^3.0.5",
    "uuid": "^10.0.0",
    "xmlhttprequest": "^1.8.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
